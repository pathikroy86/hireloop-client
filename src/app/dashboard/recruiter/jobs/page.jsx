import RecruiterJobsTable from "@/components/dashboard/RecruiterJobsTable";

export const dynamic = 'force-dynamic';

const RecruiterJobs = async () => {
    return <RecruiterJobsTable />;
};

export default RecruiterJobs;
